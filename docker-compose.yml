version: "3"
services:
  router:
    image: nginx:latest
    volumes:
      - ./nginx-routing/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    # restart: on-failure

    depends_on:
      - mulambdati

  mulambdati:
    extends:
      file: mulambdati/docker-compose.yml
      service: mulambdati
